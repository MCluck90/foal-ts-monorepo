{
  "private": true,
  "name": "@engine/validation",
  "version": "1.0.0",
  "exports": {
    ".": "./build/index.js",
    "./": "./build/"
  },
  "scripts": {
    "clean": "rimraf -rf ./build tsconfig.tsbuildinfo",
    "build": "tsc",
    "rebuild": "yarn run clean && yarn run build",
    "watch": "tsc -w",
    "test": "jest --watch",
    "test:all": "yarn test --watchAll=false --ci",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "prettier": "@config/prettier",
  "dependencies": {
    "@access/common": "*",
    "@access/task": "*",
    "@config/prettier": "*",
    "@types/jest": "^26.0.23",
    "@utility/common": "*",
    "jest": "26.6.0",
    "rimraf": "^3.0.2",
    "ts-jest": "26.5.6",
    "typescript": "^4.3.4"
  }
}
