{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Client",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start client",
      "postDebugTask": "Stop client"
    }
  ]
}
